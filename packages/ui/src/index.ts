// packages/ui — shared presentational components (Phase 7 scope,
// ARCHITECTURE.md §2). Placeholder in Phase 2: the workspace package exists
// so `shared -> ui -> web` (the dependency direction in §2.1) is wired from
// the start, with no components in it yet.
export const UI_PACKAGE_PLACEHOLDER = true;
